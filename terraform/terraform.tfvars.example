# Optional overrides. Do NOT put tailscale_authkey here — pass it via env:
#   set -a; source ../.env; set +a
#   export TF_VAR_tailscale_authkey="$tailscale_authkey"
# Or just use the helper: ../scripts/tf.sh apply

# aws_region          = "ap-northeast-1"
# project_name        = "tailscale-exit-node"
# instance_type       = "t4g.nano"
# root_volume_size_gb = 8
# tailscale_hostname  = "aws-exit-node"
# allow_ssm_session   = true
